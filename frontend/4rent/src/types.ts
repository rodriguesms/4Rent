export type RealStateDTO = {
    id: number
    announcementTitle: string,
    announcementDate: Date,
    city: string,
    state: string,
    price: number,
    forRent: boolean,
    status: string,
    type: string
}